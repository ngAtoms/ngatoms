---
"ngatoms": minor
---

feat(primitives): overlays (date-picker, dropdown-menu, popover, select, tooltip) now reposition on scroll instead of closing. Overlay service gains a `closeOnScroll` option; panel `z-index` normalized to 50.